# Theme + Tailwind Configuration

- Tailwind `extend` now includes:
  - `shadow.glass`
  - `backgroundImage.medical-gradient`
  - rounded design defaults for modern cards

Global utility classes in `src/index.css`:
- `.card`: glassmorphism + soft cyan border
- `.btn`, `.btn-primary`, `.btn-muted`: refined interaction states
- `.btn::after`: subtle ripple press effect
- `.input`: accessible focus treatment
